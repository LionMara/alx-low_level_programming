#include "main.h"
/**
 * _abs - the function displays the absolute value
 * @n: - the integer to be maxed
 *
 * Return: returns the absolute value
 */
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
else
{
return (0);
}
}
