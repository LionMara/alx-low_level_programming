#include "main.h"
/**
 * mul - multiplies two integers
 * @a: one of the ints
 * @b: the other integer
 * Return: return the product
 */
int mul(int a, int b)
{
int product;
product = a * b;
return (product);
}
